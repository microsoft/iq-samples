# FabricIQ Foundry Viz - Backend Dependencies
#
# Uses the Foundry Responses API (/openai/v1/responses) for OBO identity
# passthrough required by Fabric Data Agent tools. The older threads/runs
# API does NOT support OBO and fails with Fabric tools.

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# Azure AI Foundry (Responses API via direct REST)
azure-identity>=1.15.0
requests>=2.31.0

# Azure Monitor / App Insights tracing
azure-monitor-opentelemetry>=1.6.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0

# Voice Live relay
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.0
